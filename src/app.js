/**
 * Add two numbers.
 * @param {number} a
 * @param {number} b
 * @returns {number}
 */
function add(a, b) {
  return a + b;
}

module.exports = { add };

// Context loader placeholder to sync docs and agents
// TODO: implement automatic README/AGENTS/JSDoc sync
